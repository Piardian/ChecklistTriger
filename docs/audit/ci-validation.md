# CI Validation

The audit branch is validated by the repository's GitHub Actions build-and-test workflow on Node.js 20 and 22.

Historical learning changes require TypeScript typecheck plus the automated test suite before they are considered complete.
